specVersion: "v1beta"
serviceId: "yuppie-web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "yuppie-web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
